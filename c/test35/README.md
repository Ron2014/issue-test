# 共享内存

共享内存是最快的 IPC 形式，因为进程不再通过执行进入内核的系统调用来传递彼此的数据，省略了两次内存拷贝。


## 实现

实现两个进程A和B：

A 创建共享内存，从共享内存中不断读取数据，充当 server 监听端口。

B 获取共享内存，不断向共享内存中写入数据，充当 client 发送请求

实例中，printf 表示数据的读取， char 赋值表示数据写入。


## 命令

查看共享内存

ipcs -m

杀出共享内存

ipcrm -m
